closing the request window = cancel
closing main window = cancel
add request timeout
file select window should appear on top
add timeout to transfer request

differentiate sending files and receiving files dict

what happens when the receiver is stopped and the sender still sends
can you reach a vm?
ping mode - after pinging can you contact a port on the dest ip that you *haven't* pinged
process_transfer vége: send a confirmation and or check hash
close transfer windows when transfer is accepted